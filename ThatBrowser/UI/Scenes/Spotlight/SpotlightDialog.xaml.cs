using Microsoft.UI.Xaml.Controls;

namespace ThatBrowser.UI.Scenes.Spotlight;

internal partial class SpotlightDialog : ContentDialog
{
    public SpotlightDialog()
    {
        InitializeComponent();
    }
}